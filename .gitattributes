# Normalise to LF in the repository. Without this, working on Windows commits
# CRLF and the Linux CI runner sees the whole tree as modified.
* text=auto eol=lf

*.png binary
*.jpg binary
*.webp binary
*.ico binary
*.woff  binary
*.woff2 binary
